# [Claude Code 작업 지시서] 반복 업무 자동화 및 기존 업무 개선 (PDF 추출 · MES 현황 메일 · 협력업체 결품 응답 웹페이지)

- 기획자: 권도연 (동일 내용이 "익명"으로도 한 번 더 게시됨 — 중복)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 업무 자동화 스크립트 3종 + 협력업체 응답용 웹 애플리케이션 1종

---

## 0. Claude Code에게 — 작업 방식

1. 이 문서는 **4개의 독립적인 과제**를 담고 있다. 각 과제를 별도 폴더/모듈로 만들고, 과제별로 독립 실행 가능하게 한다.
2. 기획자는 **회사 시스템(SAP, MES, 회사 Outlook)에 접근할 수 없는 개인 노트북**으로 실습 중이다. 따라서 모든 사내 시스템 연동 지점은 **"교체 가능한 어댑터 + 샘플 데이터/Mock"** 으로 구현하고, 실제 연동 방법은 README에 명시한다.
3. 우선순위: **과제 D(결품 응답 웹페이지) → 과제 A(PDF 추출) → 과제 C(엑셀 가공·메일) → 과제 B(MES 메일)**. 웹페이지는 개인 환경에서 완결 가능하므로 가장 먼저 완성한다.
4. 각 과제 시작 전 9번의 해당 질문을 확인하고, 답이 없으면 5번의 가정으로 진행한다.
5. 부록의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

"SAP/MES 자료 다운 → 엑셀 가공 → 메일 송부"로 반복되는 일일 업무를 자동화하고, 협력업체에 매일 보내던 D+4 결품 현황 엑셀을 **업체가 직접 접속해 특이사항 유/무를 응답하는 웹페이지**로 대체한다.

## 2. 배경 및 현재 업무 상황

- 담당자의 업무 대부분이 "SAP 자료 다운 → 엑셀 가공(VLOOKUP, 피벗) → Outlook 메일 송부" 패턴이다.
- 엑셀 가공은 이미 엑셀 쿼리(Power Query) 마스터 파일(팀즈에 보관)로 일부 자동화되어 있다.
- 협력업체 결품 확인은 매일 아침 엑셀을 메일로 보내고 개별 회신·전화·문자로 확인하는 수작업이다.

## 3. 과제별 요구사항

### 과제 A. 업체별 상이한 PDF에서 데이터 추출 → 엑셀
- 입력: 협력업체들이 보내는 PDF(업체마다 레이아웃 다름).
- 추출 항목 5가지: 4가지는 업체 공통 형식(무게, 품번 등), **1가지(인코텀즈 번호)는 업체마다 표기 형식이 다름**.
- 출력: 하나의 엑셀(행 = PDF 1건, 열 = 5개 항목 + 파일명 + 업체명 + 추출 신뢰도).
- 구현 방향: 공통 4항목은 정규식/위치 기반 파서, 인코텀즈는 업체별 규칙(설정 파일)으로 분기 + 실패 시 LLM 보조 추출 옵션. 추출 실패 항목은 빈칸 + 검토 필요 플래그.
- 샘플: 레이아웃이 다른 업체 3곳 분량의 샘플 PDF를 직접 생성해 테스트.

### 과제 B. MES 라인별 진행현황 → 매일 07:00~07:30 Outlook 메일
- MES는 Chrome에서 URL 입력으로 접속하는 웹 시스템. 화면에 **"투입 계획"** 과 **"시작 일시"** 컬럼이 있음.
- 로직: 당일 07시 기준으로 계획 대비 시작 일시 차이 수량을 계산 → 라인이 **앞당겨지면 빨간색**, **밀리면 검은색**으로 메일 본문에 표기.
- 수신인/참조인 고정 목록. 일 1회 07:00~07:30 발송.
- 구현 방향: ① MES 데이터 가져오기(브라우저 자동화 Playwright 또는 엑셀 다운로드 파일 읽기 — Mock CSV로 대체) → ② 비교 로직 → ③ HTML 메일 본문 생성(색상 표기) → ④ Outlook 발송(win32com / Outlook COM; 개인 환경에서는 .eml 파일 저장 또는 미리보기 HTML로 대체) → ⑤ Windows 작업 스케줄러 등록 안내.

### 과제 C. SAP 다운로드 → 엑셀 가공 → 팀 메일 송부
- SAP에서 받은 엑셀을 팀즈의 마스터 쿼리 파일로 가공(VLOOKUP, 피벗)하는 단계는 이미 존재.
- 자동화 대상: 가공 완료된 엑셀의 **특정 범위를 이미지로 캡처해 메일 본문에 삽입 + 엑셀을 다른 이름으로 저장해 첨부 + Outlook으로 팀원에게 발송**.
- 구현 방향: Python(openpyxl/xlwings) 또는 Power Automate Desktop 흐름. 개인 환경에서는 샘플 엑셀로 "캡처 이미지 + 첨부 + 메일 초안(.eml/.msg)" 생성까지 검증.

### 과제 D. 협력업체 결품(D+4 확정구간) 응답 웹페이지 ★ 우선
- 현재: 매일 오전 업체별 결품 현황 엑셀 송부 → 메일/전화/문자로 개별 확인.
- 목표:
  1. 담당자가 당일 결품 현황 엑셀을 업로드하면 업체별 페이지에 자동 반영.
  2. 업체 담당자가 접속(업체 코드 + 간단 인증) → **본인 담당 부품 목록**만 표시 → 부품별 **[특이사항 있음] / [없음]** 버튼 클릭, '있음'이면 사유·예상 입고일 입력.
  3. 담당자 화면: 업체별 응답 현황 모니터링(미응답/이슈 있음/이상 없음), 이슈 부품 목록 → 생산계획 변경 검토용 내보내기.
  4. **10시까지 미응답 업체의 담당자에게 카카오톡 알림 발송** (개인 환경에서는 알림 로그/이메일/웹 푸시로 대체, 카카오 알림톡 연동은 어댑터로 분리).
- 구현 방향: 웹 앱(프론트 + 경량 백엔드 또는 Supabase). 엑셀 업로드 파서, 업체/부품/응답 테이블, 마감시간 체크 스케줄러.

## 4. 데이터 스키마 (샘플 데이터 생성 지침)

**결품 현황 엑셀 (과제 D)**: 일자, 업체코드, 업체명, 담당자명, 담당자 연락처, 품번, 품명, 필요수량, 확보수량, 결품수량, 확정구간(D+1~D+4), 라인.

**MES 현황 (과제 B, Mock CSV)**: 라인, 작업지시번호, 품번, 투입 계획(일시), 시작 일시, 계획수량, 실적수량.

**PDF 추출 결과 (과제 A)**: 파일명, 업체명, 품번, 무게, 항목3, 항목4, 인코텀즈 번호, 검토필요 여부.
(항목 3·4의 실제 명칭은 확인 필요 — 9번 참조)

## 5. 제약사항 및 가정

- 사내 시스템(SAP/MES/Outlook/팀즈) 연동은 개인 노트북에서 불가 → Mock/샘플로 대체하고 실제 연동 코드는 분리된 어댑터로 작성.
- 카카오톡 발송은 알림톡(비즈메시지) 사업자 계약이 필요 → 1차는 이메일/SMS 대체 또는 로그로만 기록.
- 웹페이지는 사외 협력업체가 접속하므로 인터넷 공개 배포 가능한 구조(Vercel/Netlify + Supabase 등)로 가정.
- 보안 중요도는 중간: 업체 코드 + 비밀번호(또는 매일 발급되는 링크 토큰) 수준의 인증.

## 6. 권장 기술 스택 (제안)

- 과제 D: HTML/CSS/JS(또는 React/Next.js) + Supabase(DB·Auth) 또는 Node/Express + SQLite. 엑셀 파싱 SheetJS.
- 과제 A: Python (pdfplumber/PyMuPDF + 정규식 + pandas → openpyxl). 선택적으로 LLM API로 인코텀즈 보조 추출.
- 과제 B·C: Python (pandas, openpyxl, win32com Outlook), Playwright(MES 스크래핑). 또는 Power Automate Desktop.

## 7. 산출물

1. `taskA_pdf_extract/` — 스크립트, 업체별 규칙 설정 파일, 샘플 PDF 3종, 결과 엑셀
2. `taskB_mes_mail/` — Mock CSV, 비교 로직, HTML 메일 템플릿(빨강/검정 표기), Outlook 발송 모듈, 스케줄러 등록 가이드
3. `taskC_sap_excel_mail/` — 엑셀 범위 캡처 + 첨부 + 메일 초안 생성 스크립트
4. `taskD_shortage_web/` — 웹 앱 소스, 샘플 엑셀, 배포 가이드, 10시 미응답 알림 스케줄러
5. 전체 README (회사 PC에서 실제 연동 시 수정 지점 명시)

## 8. 구현 단계

- Phase 1: 과제 D 핵심 흐름 (엑셀 업로드 → 업체 페이지 → 있음/없음 응답 → 담당자 모니터링)
- Phase 2: 과제 D 마감 알림(10시) + 내보내기 / 과제 A PDF 추출기
- Phase 3: 과제 B MES 메일, 과제 C 엑셀 캡처·첨부 메일 (Mock 기반)
- Phase 4: 실제 연동 어댑터 가이드, README, 배포

## 9. 확인 필요 사항 (작업 전 질문)

1. (A) PDF에서 추출하는 5개 항목의 정확한 이름은? (무게, 품번, ?, ?, 인코텀즈 번호) 업체 수는 몇 곳인가?
2. (A) 인코텀즈 번호의 업체별 표기 예시 2~3개를 알려줄 수 있는가?
3. (B) MES에서 데이터를 엑셀로 내려받을 수 있는가, 화면에서만 볼 수 있는가?
4. (B) "차이나는 수량" 계산 기준(계획수량 − 시작된 수량? 시간 차이?)과 메일 표 형식 예시?
5. (C) 캡처할 엑셀 범위와 메일 수신 팀원 수?
6. (D) 업체 담당자 로그인 방식 선호(업체코드+비밀번호 / 매일 발송되는 일회용 링크)?
7. (D) "10시 미응답" 알림은 카카오톡이 필수인가, 이메일/SMS로 시작해도 되는가?
8. (D) 배포 환경: 인터넷 공개 가능한가, 사내망만 가능한가?

---

## 부록. 기획서 원문 (Padlet 게시 내용 그대로)

반복 업무 자동화 및 기존 업무 개선

업무 자동화 필요사항

반복 업무

1. 업체들마다 다른 형식으로 보내는 PDF 파일에서 필요 정보들을 엑셀로 데이터 추출
상황 : 5가지 정보들을 추출하면 4가지는 데이터 형식이 똑같음(무게, 품번 등), 1가지는 업체마다 형식이 다름 (인코텀즈 넘버)

2. MES(크롭에서 사이트 주소 입력식으로 접속)에서 라인별 진행현황을 파악 후 outlook으로 지정된 수신인, 참조인에 메일 전달
1) 주기 : 일 1회(07~07시 30분 메일 발송 필요) 라인
2) 현황 파악 방법: MES에 투입 계획과 시작 일시라는 열이 존재, 당일 07시 기준 차이나는 수량에 따라 라인이 당겨가고 있으며 빨간색 표시, 밀리면 검은색 표시로 메일에 기입 필요

3. SAP에서 자료다운 -> 엑셀 가공 -> 메일 발송
SAP에서 자료 다운 -> 엑셀가공 부분은 엑셀 쿼리(엑셀 쿼리 파일은 팀즈에 마스터 파일이 있음)로 자동화(Vlookup, 피벗 작업 등) -> Outlook으로 팀분들께 메일 송부(엑셀 내용 캡쳐 첨부, 엑셀 파일 다른이름으로 저장후 첨부)

*전체적으로 제가 하는 업무들이 SAP 자료 다운 -> 엑셀 가공->메일 송부라 위에 로직을 제대로 배우면 사용할 수 있는 부분들이 매우 많을 것 같습니다….

새로 시도해보고 싶은 업무

1. 매일 오전 협력업체에 확정구간(D+4) 결품 현황에 대해 엑셀 파일 송부 -> 개별적으로 메일로 회신 및 전화 or 문자로 확인
1) 개선 : AI 이용한 웹페이지 제작 -> 업체 본인 담당부품들에 대해서 특이사항 있음, 없음 버튼 누름->이슈 있음 부품 모니터링 및 생산계획 변경, 10시까지 응답 안할경우 업체 담당자에 카톡 발송

*위에 부분들이 다 회사 컴퓨터로 해야하는 것들이라 이번 실습때 가능한지는 모르겠네요…현재 개인 노트북으로 교육 참석 중입니다.(회사 노트북 지참은 하고 있지만 보안으로 불편해 개인 노트북으로 참석 중)
